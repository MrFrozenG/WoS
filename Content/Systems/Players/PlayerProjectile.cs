using Terraria.ID;
using Terraria.ModLoader;
using Terraria;
using Microsoft.Xna.Framework;

namespace WoS.Content.ModPlayers
{
    public class PlayerProjectile : GlobalProjectile
    {
        /*
        public override bool InstancePerEntity => true;
        public bool fromKeyofReason = false;

        public override void PostAI(Projectile projectile)
        {
            Player player = Main.player[projectile.owner];

            if (player.GetModPlayer<MainPlayer>().FrostStone && projectile.CountsAsClass(DamageClass.Melee) && Main.rand.NextBool(3) && projectile.CountsAsClass(DamageClass.SummonMeleeSpeed))
            {
                Dust.NewDust(projectile.position, projectile.width, projectile.height, DustID.IceTorch, projectile.velocity.X, projectile.velocity.Y, 100, default(Color), 2f);

            }
        }
        */
    }
}
